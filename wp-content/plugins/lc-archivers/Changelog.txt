Lc.Archivers开发日志:

V1.2.0
* 可绑定域名

V1.1.2
* 修复自动更新时没有同步更新 标签页 的问题。

V1.1.1
* 修复自动更新时没有同步更新 标签页 的问题。

V1.1.0
调整自动更新的时间。
调整自动更新时目录没有同步创建的BUG。

V1.0.9
加强支持HTML代码。

V1.0.8
支持HTML代码。这样你可以在底部加入自己版权信息

V1.0.7
去掉版权信息。只留首页。多多支持。

V1.0.0 (2009-08-22)

但当用户通过搜索引擎提供的链接访问 Archiver 时, Lc.Archivers 会自动将用户导向到论坛的动态页面版本.

Lc.Archivers 不是给人看的，是给搜索引擎看的。

URL简单唯一,利于索引
页面干净，信噪比高：这是显而易见的，也是最重要的优点
